<?php
$servername = "localhost";
$username = "tu_usuario";
$password = "tu_password";
$dbname = "registro_equipos";

$conn = new mysqli($servername, $username, $password, $dbname);

if ($conn->connect_error) {
    die("Conexión fallida: " . $conn->connect_error);
}
?>


//Conexion php//
